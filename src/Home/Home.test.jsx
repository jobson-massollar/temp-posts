import { render, screen } from '@testing-library/react';
import Home from '.';

describe('< Home />', () => {

  it('should always pass 1', () => {
    expect(1).toBe(1);
  });

  it('should always pass 2', () => {
    expect(1).toBe(1);
  });

  it('should always pass 3', () => {
    expect(1).toBe(1);
  });
});
